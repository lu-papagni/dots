-s | <directory_fonte> | La directory che contiene gli elementi da linkare. NB: la destinazione e' `~/.config`.
|| La fonte in questo caso viene usata per dedurre i giusti collegamenti simbolici da eliminare.
-f | <file_filtro> | Specifica un file contenente regole per ignorare specifici file e directory.
|| Usa solo espressioni regolari semplici.
