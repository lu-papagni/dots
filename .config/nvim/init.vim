set number " attiva numeri di riga
set relativenumber " numeri relativi
set noruler " nascondi indicatore posizione nel file di default
set noshowmode " nascondi indicatore modalità di inserimento di default
set nowrap " non tornare a capo con le righe se la finestra non può contenerle

" Dimensione dei rientri (2 spazi)
set tabstop=2
set shiftwidth=2
set expandtab

" Inizializzazione moduli
lua require('luca')
